export interface FilterState {
  shop: string | null; // Antes era 'store'
  price_range: string | null; // Antes era 'price'
  type_parfum: string | null; // Antes era 'type'
  audience: string | null;
}
